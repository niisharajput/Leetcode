class Solution {
public:
    bool isPalindrome(int x) {
        if(x<0){
            return false;
        }
       long num = x;
       long digit = 0;
while(x!=0){
       int rem = x%10;
       
       digit  = digit*10+rem;
       x/=10;
       

}
        return num ==digit;
        
    }
};
